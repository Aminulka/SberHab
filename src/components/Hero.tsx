import React from 'react';
import '../styles/hero.css';

const Hero: React.FC = () => {
  return (
    <div className='hero'>
      <div className='hero-content'>
        <button>найди свою фактуру</button>
      </div>
    </div>
  );
};

export default Hero;
